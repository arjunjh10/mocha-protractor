{
  "name": "mocha-protractor",
  "version": "1.0.0",
  "description": "e2e project framework to get started",
  "author": "Arjun Jhawar <arjun.jhawar@gmail.com>",
  "scripts": {
    "build": "yarn rimraf typescript && tsc",
    "test": "yarn build && protractor typescript/conf/conf.js --verbose",
    "webdriver-clean": "webdriver-manager clean",
    "webdriver-update": "webdriver-manager update",
    "webdriver-start": "webdriver-manager start --standalone"
  },
  "dependencies": {
    "chai": "^4.3.4",
    "chromedriver": "^90.0.0",
    "mocha": "^8.3.2",
    "mochawesome": "^6.2.2",
    "protractor": "^7.0.0",
    "rimraf": "^3.0.2",
    "selenium-webdriver": "^4.0.0-beta.3",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "devDependencies": {
    "@types/chai": "^4.2.17",
    "@types/mocha": "^8.2.2"
  }
}
